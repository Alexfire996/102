{
  "name": "thrive-in-darkness",
  "version": "1.0.0",
  "description": "Thrive in the Darkness landing page",
  "type": "module",
  "scripts": {
    "dev": "browser-sync start --server --no-notify --directory --files '**/*.js' '**/*.css' '**/*.html'",
    "build": "cp -r . dist/",
    "vercel-build": "npm run build"
  },
  "dependencies": {
    "animejs": "^4.0.1"
  },
  "devDependencies": {
    "browser-sync": "^2.29.3"
  }
}
